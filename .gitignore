# PyInstaller
dist/
build/
RadarRift-win64.zip
*.spec.bak

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/

# Large / generated ML assets
*.pt
*.pth
*.onnx

# Milestone packages — local only, not on main
cp2/
cp3/

# Keep live / deliverable weights tracked (force-add path exceptions)
!runs/detect/continue_Teleport/weights/best.pt
!runs/classify/tp_exp_aug_faint/weights/best.pt
!runs/classify/tp_confirm_cls/weights/best.pt
!runs/classify/tp_confirm_cls/weights/best.onnx
!cache/champion_yolo11n.onnx
!cache/splash_detection.onnx

# App-local state (positions, roster)
.radarrift_pos.json
pos.json
radarrift_pos.json

# Downloaded game cache (icons, matrices) — run rebuild_cache.py
cache/
# but keep the ONNX models the app/bundle loads (see exceptions above)
!cache/
cache/*
!cache/champion_yolo11n.onnx
!cache/splash_detection.onnx

# Spec temp copy
cache_bundle_tmp/
tp_bundle_tmp/

# Training / Ultralytics
runs/
# keep active detector + TP-CNN checkpoints
!runs/
runs/*
!runs/detect/
runs/detect/*
!runs/detect/continue_Teleport/
runs/detect/continue_Teleport/*
!runs/detect/continue_Teleport/weights/
runs/detect/continue_Teleport/weights/*
!runs/detect/continue_Teleport/weights/best.pt
!runs/classify/
runs/classify/*
!runs/classify/tp_exp_aug_faint/
runs/classify/tp_exp_aug_faint/*
!runs/classify/tp_exp_aug_faint/weights/
runs/classify/tp_exp_aug_faint/weights/*
!runs/classify/tp_exp_aug_faint/weights/best.pt
!runs/classify/tp_confirm_cls/
runs/classify/tp_confirm_cls/*
!runs/classify/tp_confirm_cls/weights/
runs/classify/tp_confirm_cls/weights/*
!runs/classify/tp_confirm_cls/weights/best.pt
!runs/classify/tp_confirm_cls/weights/best.onnx
wandb/

# annotation_tool.py default layout (session/images, session/labels)
session/
session_cropped/

# Local YOLO datasets & caches (optional; large)
# Any root folder whose name starts with "dataset" (dataset, dataset_minimap, …)
dataset*/
debug_crops/
*.cache

# Extra screenshots — assets/ holds the 060822 file used by the spec
*2026-03-08 193538.png
*2026-03-08 194735.png

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
